{
  "name": "m3u8-player",
  "private": true,
  "type": "module",
  "scripts": {
    "build:experimental": "esbuild player-experimental/src/player.ts --bundle --format=esm --outfile=player-experimental/player.js",
    "typecheck:experimental": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "mediabunny": "^1.51.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
